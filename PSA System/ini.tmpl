{{$entry := sdict}}
{{$channellist := (cslice
                  .Channel.ID
                  )}} {{/* replace .Channel.ID with a list of PSA channel IDs */}}

{{range $channel := $channellist}}
    {{$entry.Set (toString $channel) (cslice currentTime 0)}}
{{end}}
{{dbSet 0 "PSA" $entry}}
